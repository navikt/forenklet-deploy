machine:
  timezone: Europe/Oslo
  node:
    version: 8.9.4

dependencies:
  override:
    - npm install


compile:
  override:
    - npm run test
    - npm run lint
    - npm run build

general:
  build_dir: src/frontend
